# cpp-cmake-git-lab
A minimal C++ project to practice Git + GitHub workflows (branches, PRs, conflicts, recovery).
## Build
```bash
cmake -S . -B build
cmake --build build
```
## Run
```bash
./build/app
```
